{
  "name": "cofracture",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build:wasm": "wasm-pack build presence-wasm --dev --weak-refs --reference-types -t bundler -d pkg",
    "build:wasm:release": "wasm-pack build presence-wasm --release -t bundler -d pkg"
  },
  "dependencies": {
    "presence-wasm": "file:presence-wasm/pkg"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.1.1",
    "vite-plugin-top-level-await": "^1.5.0",
    "vite-plugin-wasm": "^3.4.1"
  }
}
